#include<cstdio>
#include<cmath >
#include<iostream>

int main(){
	int i, j;
	for (i = 101; i <= 200; i += 2){
		for (j = 2; j <= sqrt(i); j++){
			if (i % j == 0)
				break;
		}
		if (j > sqrt(i))
			printf("%d\n", i);
	}
	system("pause");
	return 0;
}
